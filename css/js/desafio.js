// window.onhashchange = function (e) {
//     // pegando os hash antigos e novos
//     const oldURL = e.oldURL.split('#')[1]// pegando só os nomes
//     const newURL = e.newURL.split('#')[1]// pegando só os nomes
//     console.log(oldURL, newURL)// imprimindo no console para verificar 
//     const urlOld = document.querySelector(`#menuOpcoes a[href='#${oldURL}']`) // pegando a url 
//     const newUrl = document.querySelector(`#menuOpcoes a[href='#${newURL}']`) // pegando a url
//     urlOld && urlOld.classList.remove('color') // removendo um classe da url antiga 
//     newURL && newUrl.classList.add('color') // adiciona um classe a url antiga 
// }
