const notas = [1.5, 7.9, 7.9, 3.7]

for(let n in notas) { // nomeie com a variável para pode estar somente na estrutura de for 
    console.log(n + ': ' + notas[n])
}
    const pessoa = {
    nome: 'André',
    sobrenome: 'Viana', 
    peso: '71 kg',
    idade: '17 anos'
}

for(let atributos in pessoa){ // coloque variável para poder pertencer somente a estrutura de for
    console.log(`${atributos} = ${pessoa[atributos]}`)
}

// console.log(pessoa)

// muito usado com os indices de uma determina função 




















































